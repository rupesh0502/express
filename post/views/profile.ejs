<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="w-full flex justify-end mb-5">
            <a class="px-3 py-2 inline-block bg-red-500 rounded-md text-sm mb-5" href="/logout">Logout</a>
        </div>
        <div class="flex item-start gap-3">
            <div class="w-10 h-10 rounded-full overflow-hidden">
                <a href="/profile/upload"><img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic %>" alt=""></a>
            </div>
            <h1 class="text-3xl tracking-tighter"><span class="font-light">Hello,</span> <%= user.name %> 👋</h1> 
        </div>
        <h5 class="mb-5">You can create a new post</h5>
        <form action="/post" method="post">
            <textarea placeholder="What's on your mind ?" class="block p-3 w-1/2 resize-none border-2 border-zinc-800 outline-none rounded-md bg-transparent" name="content" id=""></textarea>
            <input type="submit" class="px-3 w-40 py-2 block bg-blue-500 rounded-md text-sm mt-2" value="Create new post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="postcontainer mt-5 flex flex-wrap gap-3">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-[47%] md:w-[32%] lg:w-[24%] p-4 border-[1px] border-zinc-700 bg-zinc-800 rounded-md mb-4">
                        <h5 class="text-blue-500 mb-2">@<%= user.username %></h5>
                        <p class="text-sm tracking-tight overflow-hidden"><%= post.content %></p>  
                        <small class="mt-2 inline-block"><%= post.likes.length %> Likes</small>  
                        <div class="btns flex gap-4 mt-1">
                            <a class="text-blue-500" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                            </a>
                            <a class="text-zinc-500" href="/edit/<%= post._id %>">Edit</a>
                        </div>            
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>