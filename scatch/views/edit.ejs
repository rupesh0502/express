<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit your post</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="w-full flex justify-end mb-5">
            <a class="px-3 py-2 inline-block bg-red-500 rounded-md text-sm mb-5" href="/logout">Logout</a>
        </div>
        <h5 class="mb-5">You can edit your post</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea placeholder="What's on your mind ?" class="block p-3 w-1/3 resize-none border-2 border-zinc-800 outline-none rounded-md bg-transparent" name="content" id=""><%= post.content %></textarea>
            <input type="submit" class="px-3 w-40 py-2 block bg-yellow-500 text-black rounded-md text-sm mt-2" value="Update post">
        </form>
    </div>
</body>
</html>